<!DOCTYPE html>
<html lang="ar">
<head>
  <meta charset="UTF-8">
  <title>بطاقة عيد مبارك</title>

  <!-- خط عربي جميل -->
  <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@700&display=swap" rel="stylesheet">

  <style>
    body {
      text-align: center;
      font-family: 'Tajawal', sans-serif;
      background: linear-gradient(135deg, #f5f5f5, #eaeaea);
      margin: 0;
      padding: 20px;
    }

    h1 {
      margin-bottom: 10px;
    }

    input {
      padding: 12px;
      font-size: 18px;
      width: 260px;
      border-radius: 10px;
      border: 1px solid #ccc;
      margin: 10px;
      text-align: center;
    }

    button {
      padding: 12px 20px;
      font-size: 18px;
      border: none;
      border-radius: 10px;
      margin: 5px;
      cursor: pointer;
      background: black;
      color: white;
    }

    button:hover {
      opacity: 0.85;
    }

    canvas {
      margin-top: 20px;
      max-width: 100%;
      border-radius: 10px;
    }
  </style>
</head>

<body>

  <h1>🎉 بطاقة عيد مبارك</h1>
  <p>اكتب اسمك وابدأ المعايدة</p>

  <input type="text" id="nameInput" placeholder="اكتب اسمك هنا">
  <br>

  <button onclick="generateImage()">إنشاء البطاقة</button>
  <button onclick="downloadImage()">تحميل</button>

  <br>
  <canvas id="canvas"></canvas>

  <script>
    const image = new Image();
    image.src = "EidHazazi.png";

    const canvas = document.getElementById("canvas");
    const ctx = canvas.getContext("2d");

    image.onload = function () {
      canvas.width = image.width;
      canvas.height = image.height;
      ctx.drawImage(image, 0, 0);
    };

    function generateImage() {
      const name = document.getElementById("nameInput").value;

      if (!name) {
        alert("اكتب اسمك أولاً");
        return;
      }

      ctx.clearRect(0, 0, canvas.width, canvas.height);
      ctx.drawImage(image, 0, 0);

      ctx.fillStyle = "black";
      ctx.textAlign = "center";

      // تغيير حجم الخط حسب طول الاسم
      let fontSize = 60;
      if (name.length > 15) fontSize = 40;
      if (name.length > 25) fontSize = 30;

      ctx.font = "bold " + fontSize + "px Tajawal";

      // 👇 مكان الاسم (تقدر تعدله)
      const x = canvas.width / 2;
      const y = canvas.height * 0.72;

      ctx.fillText(name, x, y);
    }

    function downloadImage() {
      const link = document.createElement("a");
      link.download = "eid-card.png";
      link.href = canvas.toDataURL("image/png");
      link.click();
    }
  </script>

</body>